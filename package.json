{
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "name": "thinking-in-projects",
  "version": "1.0.0",
  "description": "Some demos in past projects",
  "main": "index.js",
  "scripts": {
    "start-parallel": "pnpm -r --parallel --filter='./apps/*' run dev",
    "dev": "pnpm -r --filter='./apps/*' run dev",
    "lint": "pnpm -r --filter='./apps/*' run lint",
    "prepare": "husky install",
    "lint-staged": "lint-staged"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "prettier": "^3.2.5"
  }
}
